# Build Time
With our skeleton application up and running, we can spend the rest of our time on getting deep into **Step Functions** and **BedRock**.

- We'll start by adding a Step Function to our project which will join the multiple components together.
- Then we'll add a Lambda function that will be responsible for fetching the records from the RSS feed.
- Next, we'll add the **Lambda function** that will integrate with **BedRock**.
- Finally, we'll leverage a feature of **Step Functions** that allows us to write the data dirctly to DynamoDB.

Click [here](Adding-A-Step-Function.md) to get started with adding a **Step Function** to the project.