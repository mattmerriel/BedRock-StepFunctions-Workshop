# Start at the beginning
Before we can start building things, we need to setup a few things on your local machine first. These will include:
- the AWS Command Line Interview (AWS CLI)
- The AWS Serverless Application Model CLI (AWS SAM)

Once we've done that, we can look to setup our new project and start deploying things. To get started with configuring the AWS CLI click [here](Install-AWS-CLI.md).